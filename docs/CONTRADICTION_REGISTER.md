# Relief — Contradiction Register

**Last updated:** 2026-07-12

Every material mismatch found among documentation, configuration, and code. This register exists to prevent future agents from repeating obsolete assumptions.

---

| ID | Conflicting Claims | Files | Code Evidence | Risk | Authoritative Wording | Decision Required |
|----|-------------------|-------|---------------|------|----------------------|-------------------|
| **C01** | README describes core, advanced, Plus, AI and infrastructure features as "Built ✅" | `README.md` (previous version) vs `src/utils/env.ts`, `src/screens/ListScreen.tsx` | Feature flags disable ADVANCED_FILTERS, PREMIUM, AI, EUROPE. ListScreen uses hardcoded MOCK_FACILITIES. No backend connected. | Users/agents believe features are production-ready when they cannot function. | All features reclassified using status vocabulary (BACKEND-DEPENDENT, MOCKED, UI IMPLEMENTED). | Rewrite README with honest status (completed this pass). |
| **C02** | "No login required for basic Need One Now search" vs auth-gated navigation | `docs/Security, Privacy & Trust Addendum.md` vs `src/navigation/AppNavigator.tsx` | Root navigator checks `getCurrentSession()`; unauthenticated → LoginScreen. No path to map without auth. | Urgent-use promise broken; accessibility policy violated. | Navigation currently requires authentication for all discovery. This is a **BLOCKER** for the core mission. | Must decide: add unauthenticated browse path, or update security policy. |
| **C03** | README mentions Mapbox; code uses Google Maps provider | `README.md` (previous), `.env.example` vs `app.json`, `MapScreen.tsx` | `app.json` maps `$EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN` into `googleMapsApiKey` and `googleMaps.apiKey`. `MapScreen.tsx` imports `PROVIDER_GOOGLE`. | Map may fail to render; confusing for developers. | Code uses Google Maps via `react-native-maps`. Environment variable is confusingly named `MAPBOX_ACCESS_TOKEN`. | Rename env variable or switch mapping provider. |
| **C04** | "AI recommendations" vs deterministic scoring algorithm | `README.md` (previous) vs `services/aiRecommendations.ts` | Scoring is weighted arithmetic: preference match (40%) + rating (25%) + distance (20%) + open (10%) + freshness (5%). No model, no learning, no external AI service. Feature flag `AI: false`. | Misleading to users; "AI" implies capabilities that don't exist. | Rename to "Smart Recommendations". Remove AI branding until model-backed. | Rename feature; decide if/when to add ML. |
| **C05** | "Offline maps" vs offline facility data | `README.md` (previous) vs `services/offlineMaps.ts` | Downloads facility JSON records to SQLite. No map tiles downloaded. No offline map rendering. | User downloads "maps" expecting map tiles; gets only text records. | Rename to "Offline Facility Data". Document that map tiles are not available offline. | Rename feature or implement map tile download. |
| **C06** | Route planning described as functional vs straight-line only | `README.md` (previous) vs `services/routePlanning.ts` | Uses Haversine formula (straight-line distance). Assumes 80 km/h average speed. Interpolates points along straight line. No road network. | Unsuitable for actual route planning; misleading to users. | Feature is **CLIENT LOGIC IMPLEMENTED** using straight-line approximation. Road-aware routing requires external API. | Select routing provider; implement road-aware routing before launch. |
| **C07** | 3-tab maximum (accessibility policy) vs 4 tabs implemented | `docs/Accessibility & Simplicity Addendum.md` vs `src/navigation/AppNavigator.tsx` | Navigation has 4 tabs: Map, List (Nearby), Favourites, Profile. Accessibility policy states "No more than 3 main tabs." | Accessibility policy violation. | Current implementation has 4 tabs. | Decide: reduce to 3 tabs, or update accessibility policy. |
| **C08** | "Maps, Search, Ratings, Directions" described as both free and paid Basic Access | `README.md` (previous) — lists core features as free AND Basic Access as £1.99 lifetime | `services/revenuecat.ts` defines tiers: 'free', 'basic', 'plus'. | Pricing confusion for users. | Core map access must be free (no purchase required). Basic Access is a lifetime purchase for premium features. | Clarify pricing model; align code and documentation. |
| **C09** | "Supabase migrations and seed data" marked complete (Phase 0.4) | `docs/PLAN.md` vs reality | Migrations exist as SQL files but are not deployed. No Supabase project exists. No seed data exists. | Agents may skip backend setup assuming it's done. | Reclassified: migration files exist (PLANNED); deployment and seed data are BLOCKED. | Create Supabase project; apply migrations. |
| **C10** | "ESLint, Prettier configured" marked complete (Phase 0.2) | `docs/PLAN.md` vs `package.json` | No ESLint dependency. No Prettier dependency. No lint/format scripts in package.json. | Code quality tools assumed present but missing. | Reclassified: no linting or formatting tooling configured. | Add ESLint + Prettier to devDependencies. |
| **C11** | what3words: real API vs simulated fallback | `services/locationSharing.ts` | Without `EXPO_PUBLIC_W3W_API_KEY`, returns simulated words from coordinate hash. Words are deterministic but not real W3W addresses. | **Safety risk** — simulated location codes could misdirect users in emergency. | what3words is MOCKED when no API key. Simulation must be removed or clearly labelled in UI. | Either configure W3W API key or remove the feature until one is available. |
| **C12** | Photo moderation described vs no processing pipeline | `services/community.ts` | `photo_moderation` insert sets `exif_stripped: false` and `faces_blurred: false`. No server-side processing exists to set these to true. | **Privacy risk** — location metadata in photos; identifiable faces. | Photo moderation queue exists; media processing pipeline does not. | Design and implement server-side EXIF stripping and face blurring. |
| **C13** | Alerts described as "Smart Alerts" vs local foreground polling | `services/notificationAlerts.ts` | `checkFavouriteFacilityAlerts()` polls on foreground. 1-hour cooldown in memory only (lost on app restart). No background processing. No push server. | Missed alerts; users expect background notifications. | Alerts are local foreground polling only. Not background-capable. | Implement server-triggered push notifications after backend exists. |
| **C14** | Duplicate PLAN.md files — root `docs/PLAN.md` vs `relief-app/PLAN.md` | Both files | `relief-app/PLAN.md` still titled "Project Placename". Root `docs/PLAN.md` is the authoritative version. | Confusion about which plan is current. | `relief-app/PLAN.md` replaced with pointer to `docs/ROADMAP.md`. `docs/PLAN.md` retained as historical reference. | Resolved in this documentation pass. |
| **C15** | HourWise specification unrelated to Relief | `docs/archive/HourWise Platform Specification.md` | Document describes a different platform entirely. | Confusion for new contributors. | Archived with non-authoritative banner. | Resolved. |
| **C16** | "Placename" naming in `relief-app/PLAN.md` | `relief-app/PLAN.md` line 1 | Title: "Project 'Placename' — Agent Plan & Progress" | Legacy project identity persists. | Replaced with pointer to current docs. | Resolved. |
| **C17** | Environment defaults to empty string without validation | `src/utils/env.ts` | `SUPABASE_URL = process.env.EXPO_PUBLIC_SUPABASE_URL \|\| ''` — all required services default to empty string. No startup validation. | App may silently fail with empty config rather than showing clear error. | Environment variables lack startup validation. | Add startup config validation with clear error messages. |
| **C18** | Secure Store dependency present but no sensitive data usage verified | `package.json` includes `expo-secure-store`; `app.json` lists it as plugin | No code found using `expo-secure-store` for token storage. Supabase client uses `persistSession: true` (AsyncStorage default). | Auth tokens may be stored in AsyncStorage (unencrypted) instead of Secure Store. | Token storage mechanism unverified. | Audit token storage; ensure Secure Store is used for sensitive data. |
